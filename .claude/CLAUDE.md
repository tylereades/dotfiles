# Global notes

Detailed context lives in `~/.claude/context/*.md`, one file per topic. This
file is just an index — read the linked file when its topic is relevant,
rather than trying to work from the one-line summary alone.

- **Dotfiles** — bare-repo-as-`$HOME` git pattern, Mackup split, two-machine
  sync gotchas. Relevant for anything touching `~/.zshrc`, `~/.config`,
  `~/Brewfile`, or adding a new app's config to source control.
  See [context/dotfiles.md](context/dotfiles.md).

- **Glove80 keyboard** — ZMK firmware keybinding customization.
  Relevant for anything about Tyler's keyboard layout/firmware.
  See [context/glove80.md](context/glove80.md).
